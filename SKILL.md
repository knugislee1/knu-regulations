---
name: knu-regulations
description: 경북대학교 규정·예규·지침·세칙·학칙·정관 547건(2026-09-09 스냅샷)의 전문 텍스트 DB를 검색해 답하는 스킬. "경북대 규정", "학칙", "예규", "지침", "휴학/복학/징계/장학/연구비/교원 임용/조교/강사/대학원 학위" 등 경북대학교 학사·인사·연구·재정 규정에 관한 질문, "몇 조에 있어?", "언제 개정됐어?", "규정 원문 보여줘" 요청에 사용. 답변은 반드시 DB에서 조문 원문을 찾아 규정명·조번호·번호·개정일을 인용해 작성한다.
---

# 경북대학교 규정·예규 DB 스킬

이 폴더의 `knu_reg.py`가 SQLite DB(`knu_regulations.sqlite`)를 질의한다. DB가 없으면 첫 실행 때 `data/`에서 자동 생성된다(30초 안팎).
DB는 2026-09-09에 원본 한글(.hwp) 파일 547개에서 추출한 스냅샷이다. 그 뒤의 개정은 반영되어 있지 않다.

## 질의 도구

```bash
python3 "<이 폴더>/knu_reg.py" docs 휴학                # 제목에 '휴학'이 들어간 문서 (doc_id | 종류 | 번호 | 제정 | 최종개정 | 조문수 | 제목)
python3 "<이 폴더>/knu_reg.py" search 휴학 학점          # 조문 전문검색: 모든 단어 포함, 어절 접두 일치 → 없으면 부분문자열 → LIKE 순으로 자동 완화
python3 "<이 폴더>/knu_reg.py" article 학칙 45          # 「경북대학교 학칙」 제45조 전문 (문서는 doc_id나 제목 일부로 지정, 조는 45 / 제45조 / 45조의2)
python3 "<이 폴더>/knu_reg.py" show 8                   # 문서 개요 + 장·조문 목록 (--full 이면 전문)
python3 "<이 폴더>/knu_reg.py" history 8                # 제·개정 이력 (제정/개정/전부개정, 날짜, 규정·예규 번호)
python3 "<이 폴더>/knu_reg.py" grep "정당한 사유"         # 정규식으로 전체 텍스트 줄 검색 (정확한 문구 찾기)
python3 "<이 폴더>/knu_reg.py" sql "SELECT ..."         # 읽기 전용 SQL (표: documents, amendments, articles; 뷰: v_documents_unique, v_articles; FTS: documents_fts, articles_fts, articles_fts_tri)
```

Windows에서는 `python3` 대신 `python`. 옵션: `--json`(기계 판독 출력), `search --kind 규정|지침|세칙|학칙|정관`, `search --doc <doc_id>`(특정 문서 안에서만), `search --part body|appendix|attachment`, `docs --all`(중복 파일 포함).

## 응대 절차 (반드시 이 순서로)

1. **문서 후보 찾기**: 질문의 핵심 명사로 `docs <키워드>`를 실행해 관련 규정을 고른다. 상위 규정(학칙·규정)과 하위 규정(예규·지침·세칙)이 함께 나오면 둘 다 확인한다.
2. **조문 찾기**: `search <단어 2~3개>`로 조문을 찾는다. 결과가 너무 많으면 `--doc <doc_id>`로 좁히고, 없으면 동의어(휴학/휴학원/복학, 징계/처분, 강사/시간강사)로 바꿔 다시 찾는다.
3. **원문 읽기**: 답에 쓸 조문은 반드시 `article <doc> <조>`로 전문을 읽는다. 발췌(snippet)만 보고 답하지 않는다. 항·호가 이어지는 조문은 앞뒤 조문(`show <doc>`)도 확인한다.
4. **이력 확인**: "언제 바뀌었나", "최신인가" 류 질문은 `history <doc>`로 제·개정 날짜와 번호를 확인한다. 조문 안의 `<개정 2021. 4. 23.>` 표기는 그 조문의 개정 시점이다.
5. **답변 작성**: 아래 인용 형식을 지킨다. 조문에 없는 내용을 추정해 채우지 않는다. 여러 규정이 충돌하거나 해석이 갈리면 각 조문을 나란히 보여 주고 판단은 사용자에게 맡긴다.

## 인용 형식

- 규정명은 「 」로, 조문은 제N조(제목)으로 쓰고, 문서 번호와 최종 개정일을 붙인다.
  예: 「경북대학교 학칙」 제45조(휴학) 제1항 (규정 제2889호, 최종개정 2026-06-30)
- 핵심 문장은 원문 그대로 인용하고(따옴표), 요약은 그 아래에 따로 쓴다.
- 답 끝에 한 줄로 근거를 모아 적는다: `근거: 「…」 제N조; 「…」 제M조 (DB 스냅샷 2026-09-09)`.

## 주의사항

- **최신성**: 스냅샷(2026-09-09) 이후 개정은 반영되지 않았다. 중요한 결정(징계·학위·임용 등)은 경북대학교 규정집 원본으로 최종 확인하라고 안내한다.
- **한글 3.0 원본 16건**(1984~2006년 연구소 규정 등)은 변환 과정에서 표·일부 문단이 빠졌고, 복구한 조각이 문서 끝 `=== [복구 텍스트]` 아래에 있다. `show`/`article` 출력의 "추출 주의" 표시가 있으면 답에 그 한계를 밝힌다.
- **중복 파일 4건**(파일명 끝 " (1)")은 기본 검색에서 제외된다.
- 조문이 0개로 파싱된 표 형식 문서 2건은 `show <doc> --full`로 전문을 읽는다.
- DB에 없는 규정(예: 대학원 학칙 시행세칙의 특정 학과 내규)은 "이 DB에 없다"고 말하고, 규정집 사이트 확인을 권한다. 지어내지 않는다.
- 법률 자문이 아니라 규정 원문 안내임을 필요할 때 밝힌다.

## 예시

질문: "일반휴학은 몇 학기까지 가능해?"
1. `docs 학칙` → doc_id 확인 → 2. `search 휴학 기간 --doc <id>` → 3. `article <id> 45` 전문 읽기 → 4. 답변: 조문 인용 + 요약 + 근거 줄.
