# knu-regulations — AI 에이전트용 안내

이 저장소는 경북대학교 규정·예규·지침·세칙·학칙·정관 547건(2026-09-09 스냅샷)의 텍스트 DB 꾸러미다.

- 사용자가 **"이거 설치해"**라고 하면 `INSTALL.md`를 따라 `python3 install.py`를 실행하고, 출력에 `SELFTEST PASS`와 `== 설치 완료 ==`가 있는지 확인한 뒤 결과만 3~4줄로 보고한다. 되묻지 않는다.
- 규정에 관한 질문에는 `SKILL.md`의 절차(docs → search → article 원문 읽기 → history)와 인용 형식(「규정명」 제N조, 번호, 최종개정일)을 따른다.
- 셸을 쓸 수 없는 환경이면 `index.md`에서 문서를 고르고 `data/text/*.txt`를 직접 읽어 답한다.
